services:
  iron-county-tracker:
    build: .
    container_name: iron-county-tracker
    ports:
      - "8082:80"
    volumes:
      - tracker-data:/data
    restart: unless-stopped

volumes:
  tracker-data:
