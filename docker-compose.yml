services:
  iron-county-tracker:
    image: nginx:alpine
    container_name: iron-county-tracker
    ports:
      - "8082:80"
    volumes:
      - ./iron_county_daily_tracker.html:/usr/share/nginx/html/index.html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
